@PACKAGE_INIT@

# JLDConfig.cmake
# This file is generated from JLDConfig.cmake.in
# Provides the JLD package for use with find_package(JLD)

# Include the main JLD.cmake module
include("${CMAKE_CURRENT_LIST_DIR}/JLD.cmake")

# Optional: Provide imported targets for convenience
# These targets are created when add_java_library() is called
# Example:
#   add_java_library(guava GROUP com.google.guava ARTIFACT guava VERSION 32.1.2-jre)
#   # creates target jld::guava
